{% extends 'base.html.twig' %}

{% block body %}
    <h1>Wallet</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wallet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wallet.name }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ wallet.balance }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('wallet_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('wallet_edit', { 'id': wallet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
